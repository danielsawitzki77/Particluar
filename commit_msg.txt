fix: torus tapering per-vertex, remove fixed segmentation from file format (danielsawitzki77/Game-Dev-Supreme#102)

- Fix broken torus faces: compute per-vertex tube center from actual vertex
  theta angle instead of shared face-center approximation that caused
  asymmetric deformation and face inversions at taper zone boundaries
- Add diagnostic logging to GenerateWithConnections for debugging
- Make subdivision params (sides, slices, stacks, ring_segments, tube_segments)
  optional in body JSON format — they are now runtime-determined by
  ApplySubdivision in the viewer, not baked into body definitions
- Serializer no longer emits subdivision params (geometric params only)
- Update all 12 asset body files to remove fixed segmentation
- Update tests to match new format semantics
